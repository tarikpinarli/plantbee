version: "2"

run:
  relative-path-mode: gomod
  modules-download-mode: readonly
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - unused

formatters:
  enable:
    - gofumpt
    - goimports
  settings:
    gofumpt:
      module-path: plantbee-backend
    goimports:
      local-prefixes:
        - plantbee-backend
